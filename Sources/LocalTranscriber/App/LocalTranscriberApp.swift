import SwiftUI

@main
struct LocalTranscriberApp: App {
    var body: some Scene {
        WindowGroup("Lokal transkribering") {
            ContentView()
                .frame(minWidth: 920, minHeight: 640)
        }
    }
}
